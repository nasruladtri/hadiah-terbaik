# ================================
# DATABASE CONFIGURATION
# ================================
DB_USER=postgres
DB_PASS=change_me_in_production
DB_NAME=kua_dukcapil_db

# ================================
# BACKEND CONFIGURATION
# ================================
# JWT Secret Key (CRITICAL: MUST be changed in production!)
# Generate a secure secret using: node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
# Example strong secret (DO NOT use this exact value):
JWT_SECRET=vqlAMHQ+s35Yd/SbW+dKqVjkXVvIShbtwqqbtS1y1ZIqyXJ6OGhNkwjxr81uvjSvhO8keccfR7vvKVGLvRuRag==

# JWT Token Expiration (Session Duration)
# Access token expiration configured in code (15 minutes)
# Refresh token expiration configured in code (7 days)
JWT_EXPIRES_IN=15m

# Node Environment (development | production)
NODE_ENV=development

# Backend Port
PORT=3100

# CORS Origin - Comma-separated list of frontend URLs allowed to access the API
# Development example: http://localhost:2200
# Production with multiple domains: https://yourdomain.com,https://admin.yourdomain.com
# IMPORTANT: Never use wildcard (*) in production
CORS_ORIGIN=http://localhost:2200

# ================================
# FRONTEND CONFIGURATION
# ================================
# Backend API Base URL
# Development example: http://localhost:3100/api/v1
# Production example: https://api.yourdomain.com/api/v1
VITE_API_BASE_URL=http://localhost:3100/api/v1

# ================================
# CLOUDFLARE TUNNEL
# ================================
# Cloudflare Tunnel Token (Get from Cloudflare Zero Trust dashboard)..
TUNNEL_TOKEN=your_tunnel_token_here
